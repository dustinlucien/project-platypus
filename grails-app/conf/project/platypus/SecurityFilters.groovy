package project.platypus

class SecurityFilters {

    def filters = {
        all(controller:'*', action:'*') {
            before = {
                
            }
            after = {
                
            }
            afterView = {
                
            }
        }
    }
    
}
